# Copy this file to `.env.local` and fill in real values (or set them in your
# host's environment variables for production). `.env.local` is git-ignored.

# WhatsApp number that receives orders from checkout.
# Format: country code + number, digits only — no +, spaces, or dashes.
NEXT_PUBLIC_WHATSAPP_NUMBER=910000000000
